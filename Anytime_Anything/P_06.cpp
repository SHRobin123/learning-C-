#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t;
    cin >> t;

    while ((t--) > 0)
    { // t times
        int n;
        cin >> n;

        cout << (n - 1) << "\n";
    }

    return 0;
}